

import 'dart:ui';

void calculate(){
         final RSTransform transform = RSTransform.fromComponents(
           rotation: 0,
           scale: 1.0,
           anchorX: 5.0,
           anchorY: 5.0,
           translateX: 100,
           translateY: 100,
         );
}