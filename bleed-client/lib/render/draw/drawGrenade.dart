
import 'package:bleed_client/draw.dart';

void drawGrenade(double x, double y, double z){
  double size = 4;
  double shift = shiftScale(z);
  // engine.draw.text(x, y, size / shift);
  // engine.draw.text(x, y + shiftHeight(z), shift * size, Colors.white);
}