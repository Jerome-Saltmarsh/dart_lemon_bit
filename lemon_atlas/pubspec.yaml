name: lemon_atlas
description: Compiles sprites into an atlas
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: '>=3.0.6 <4.0.0'

dependencies:
  file_picker: ^5.3.3
  lemon_watch: ^1.1.1
  image: ^4.0.17

  lemon_widgets:
    path: ..\lemon_widgets

  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
